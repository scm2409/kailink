<?xml version="1.0" encoding="utf-8"?>
<!-- ONLY emulatorDebug (E2E test infrastructure): cleartext HTTP to the local
     Matrix homeserver (Conduit :6167) and push gateway (ntfy :8090).
     Release/debug remain untouched (default: no cleartext). -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">192.168.42.20</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
